import { OptionMarkerPipe } from './option-marker.pipe';

describe('OptionMarkerPipe', () => {
  it('create an instance', () => {
    const pipe = new OptionMarkerPipe();
    expect(pipe).toBeTruthy();
  });
});
